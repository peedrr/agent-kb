# Path Guard Rails — error cases for invalid paths

-- raw-prefix-input.md --
---
type: note
title: Raw Test
---
Content
-- raw-prefix-input.md --

-- traversal-input.md --
---
type: note
title: Traversal
---
Content
-- traversal-input.md --

-- absolute-input.md --
---
type: note
title: Absolute
---
Content
-- absolute-input.md --

-- no-ext-input.md --
---
type: note
title: No Extension
---
Content
-- no-ext-input.md --

-- append-input.md --
Appended content
-- append-input.md --

# Setup: initialize a KB
exec akb init test-kb
cd test-kb

# --- raw/ prefix on write ---
! exec akb write raw/notes/test.md <../raw-prefix-input.md
stderr 'akb raw write'

# --- raw/ prefix on read ---
! exec akb read raw/notes/test.md
stderr 'akb raw read'

# --- raw/ prefix on delete ---
! exec akb delete raw/notes/test.md
stderr 'akb raw delete'

# --- raw/ prefix on append ---
! exec akb append raw/notes/test.md <../append-input.md
stderr 'akb raw write'

# --- .. in path on write ---
! exec akb write ../etc/passwd.md <../traversal-input.md
stderr 'must not contain'

# --- .. in path on read ---
! exec akb read ../etc/passwd.md
stderr 'must not contain'

# --- Absolute path on write ---
! exec akb write /tmp/evil.md <../absolute-input.md
stderr 'must be relative'

# --- Absolute path on read ---
! exec akb read /tmp/evil.md
stderr 'must be relative'

# --- No .md extension on write ---
! exec akb write mypage.txt <../no-ext-input.md
stderr 'must end with .md'

# --- Page not found on read ---
! exec akb read notes/nonexistent.md
stderr 'page not found'

# --- Page not found on delete ---
! exec akb delete notes/nonexistent.md
stderr 'page not found'

# --- Page not found on append ---
! exec akb append notes/nonexistent.md <../append-input.md
stderr 'page not found'